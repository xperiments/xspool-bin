{
  "name": "xspool-bin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-bdd": "mkdir tmp && node scripts/bbl-parser.js && node scripts/cfs-parser.js && node scripts/tiger-parser.js && node scripts/bdd-pack.js && rm -rf tmp"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "type": "module",
  "devDependencies": {
    "axios": "^1.8.4"
  }
}
